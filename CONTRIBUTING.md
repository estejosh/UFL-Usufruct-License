# Contributing

Changes to `LICENSE.txt` itself need discussion before a PR — it's legal
text, not code, so open an issue describing the proposed wording change
and why first; changes to `README.md`, `WHITEPAPER.md`, `generate.sh`,
or `generate.js` are normal PRs and go through the usual review.
